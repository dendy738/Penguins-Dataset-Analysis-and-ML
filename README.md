Penguins dataset analysis, cleaning (missing values, duplicates) and preprocessing data (Feature engineering, Scaling, One Hot Encoding), ML process with using pipelines, cross-validation, setting hyperparameters.

Results: 2 models (RandomForestClassifier, KNeighborsClassifier) without overfitting.

  Test results:
  
    RadomForest: 0.75
    
    KNN: 1.0
 
    
